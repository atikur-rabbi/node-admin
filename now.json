{
    "version": 2,
    "builds": [{
        "src": "dist/*.js",
        "use": "@now/node",
        "config": {
            "helpers": false
        }
    }],
    "routes": [{
        "src": "/.*",
        "dest": "dist/.*"
    }]
}